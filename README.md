# Mister-JP.github.io

Source for my GitHub Pages library site (MkDocs Material).

## Quick start
1) Install MkDocs Material:
   - `python -m pip install mkdocs-material`
2) Serve locally:
   - `mkdocs serve`
3) Build site:
   - `mkdocs build`
