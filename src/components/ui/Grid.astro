---
type GridCols = 'auto' | '2' | '3';
type GridGap = 'md' | 'lg';

interface GridProps {
  cols?: GridCols;
  gap?: GridGap;
  minItemWidth?: string;
  class?: string;
  [key: string]: unknown;
}

const {
  cols = 'auto',
  gap = 'md',
  minItemWidth = '16rem',
  class: className = '',
  ...rest
} = Astro.props as GridProps;

const style = cols === 'auto'
  ? `--grid-template: repeat(auto-fit, minmax(min(100%, ${minItemWidth}), 1fr));`
  : undefined;
---

<div class:list={['grid', `grid--${cols}`, `grid--gap-${gap}`, className]} style={style} {...rest}>
  <slot />
</div>
