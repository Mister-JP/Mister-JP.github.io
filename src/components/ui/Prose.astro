---
interface ProseProps {
  as?: string;
  class?: string;
  [key: string]: unknown;
}

const {
  as: Tag = 'div',
  class: className = '',
  ...rest
} = Astro.props as ProseProps;
---

<Tag class:list={['prose', className]} {...rest}>
  <slot />
</Tag>
