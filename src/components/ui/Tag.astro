---
type TagTone = 'default' | 'accent' | 'muted';

interface TagProps {
  tone?: TagTone;
  class?: string;
}

const {
  tone = 'default',
  class: className = '',
} = Astro.props as TagProps;
---

<span class:list={['tag', `tag--${tone}`, className]}>
  <slot />
</span>
