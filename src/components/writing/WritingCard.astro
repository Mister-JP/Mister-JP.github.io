---
import Button from '../ui/Button.astro';
import Card from '../ui/Card.astro';
import Tag from '../ui/Tag.astro';
import { getWritingFeatureImage } from '../../lib/feature-images';

type WritingKind = 'case-study' | 'method';

interface RelatedProjectLink {
  href?: string;
  title: string;
}

interface WritingCardProps {
  title: string;
  summary: string;
  featureImage?: string;
  kind: WritingKind;
  status: string;
  tags?: string[];
  featured?: boolean;
  href: string;
  relatedProjects?: RelatedProjectLink[];
}

const {
  title,
  summary,
  featureImage,
  kind,
  status,
  tags = [],
  featured = false,
  href,
  relatedProjects = [],
} = Astro.props as WritingCardProps;

const actions = [{ label: 'Open entry', href }]
  .filter((action): action is { label: string; href: string } => Boolean(action))
  .map((action) => ({
    ...action,
    isExternal: action.href.startsWith('http'),
  }));

const kindLabel = kind === 'case-study' ? 'Case Study' : 'Method';
---

<Card
  variant={featured ? 'soft' : 'default'}
  interactive={actions.length > 0}
  class="writing-card"
>
  <div slot="title" class="writing-card__title">
    <div class="writing-card__meta">
      <Tag tone={kind === 'case-study' ? 'accent' : 'default'}>{kindLabel}</Tag>
      <Tag tone="muted">{status}</Tag>
      {featured && <Tag tone="accent">Featured</Tag>}
    </div>
    <h3>
      <a class="writing-card__title-link text-link" href={href}>{title}</a>
    </h3>
  </div>

  <div class="writing-card__preview">
    <img src={getWritingFeatureImage(featureImage)} alt={`${title} preview`} loading="lazy" />
  </div>

  <p>{summary}</p>

  {relatedProjects.length > 0 && (
    <p class="writing-card__related">
      {relatedProjects.length === 1 ? 'Related project: ' : 'Related projects: '}
      {relatedProjects.map((project, index) => (
        <span>
          {index > 0 && <span>, </span>}
          {project.href ? (
            <a class="text-link" href={project.href}>{project.title}</a>
          ) : (
            <span>{project.title}</span>
          )}
        </span>
      ))}
    </p>
  )}

  {tags.length > 0 && (
    <div class="writing-card__tags" aria-label={`${title} tags`}>
      {tags.map((tag) => (
        <Tag tone="muted">{tag}</Tag>
      ))}
    </div>
  )}

  {actions.length > 0 && (
    <div slot="actions">
      {actions.map((action, index) => (
        <Button
          variant={index === 0 ? 'primary' : 'secondary'}
          href={action.href}
          target={action.isExternal ? '_blank' : undefined}
          rel={action.isExternal ? 'noreferrer' : undefined}
        >
          {action.label}
        </Button>
      ))}
    </div>
  )}
</Card>
